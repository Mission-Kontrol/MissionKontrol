<table class="table table-hover activities-history--table <%= locals[:hide] ? 'hide' : '' %>%">
  <tbody>
    <% activities.each do |activity| %>
      <%= render 'activities/activity', activity: activity, locals: { link: locals[:link] } %>
    <% end %>
  </tbody>
</table>